import { ZodRawShape } from "zod";
// import { HandlerProps, HandlerPropsWithBody } from "../types";

// export const isHandlerWithBody = <T extends ZodRawShape = any>(handler: HandlerProps):
//     handler is HandlerPropsWithBody<T> =>
//     !!(handler as HandlerPropsWithBody<T>).validate